<?php
function sf_second_theme_suggestions_block_alter(&$suggestions, $variables) {
  foreach ($suggestions as &$suggestion) {
    $suggestion = str_replace('sf_second_', 'bartik_', $suggestion);
  }
}